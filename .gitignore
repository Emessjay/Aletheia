# Bundled corpus (built by tools/ingest from data/sources/) — ignore the
# subdirectories of fetched raw data but keep the in-repo licensing notes.
data/sources/*/
data/sources/*.txt
data/sources/*.json
data/sources/*.xml
data/sources/*.zip
data/Aletheia.sqlite
data/Aletheia.sqlite-*

# Node / Vite
node_modules
dist
dist-ssr
*.local
.vite
tsconfig.tsbuildinfo

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Swift Package Manager (tools/ingest)
.build/
.swiftpm/
Package.resolved

# Python venv used by tools/sword-extract
.venv/
__pycache__/
*.pyc

# Editors
.vscode
.idea

# OS
.DS_Store
*.log

# Auditor system per-worker state (see scripts/spawn-worker.sh, AUDITOR.md)
.auditor-state/
